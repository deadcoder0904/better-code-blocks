export const meta = {
	title: 'Better Code Blocks with Next.js, Tailwind CSS & MDX',
	description: 'Build a beautiful code block using Next.js, Tailwind CSS& MDX.',
	date: '2021-06-01T14:54:37.229Z',
	tags: ['code blocks', 'next.js', 'mdx', 'tailwindcss'],
	author: 'Akshay Kadam (A2K)',
	published: true,
}

import unsplash from './andre-benz-JnB8Gio4GZo-unsplash.jpg'

Code blocks, but better. There are many features:

- Syntax highlight
- Apply multiple themes
- Highlight specific lines
- Highlight specific words
- Interact with the content
- Make specific words link to other pages
- Show line numbers
- Make it collapsible/expandable
- Render a preview

<!-- more -->

# Different Themes using Tailwind CSS

<!-- <img alt="Unsplash Dark" src="/andre-benz-JnB8Gio4GZo-unsplash.jpg" /> -->

<img alt="Unsplash Dark" src={unsplash} />

We have a few different themes on our code blocks.

## Default Theme

```jsx theme=turq showLineNumbers=true
import React from 'react'

export function Counter({ initialCount = 0 }) {
	const [count, setCount] = React.useState(initialCount)
	return (
		<button type="button" onClick={() => setCount((prevCount) => prevCount + 1)}>
			{count}
		</button>
	)
}
```

## Orange Theme

```jsx theme=orange
import React from 'react'

export function Counter({ initialCount = 0 }) {
	const [count, setCount] = React.useState(initialCount)
	return (
		<button type="button" onClick={() => setCount((prevCount) => prevCount + 1)}>
			{count}
		</button>
	)
}
```

## Pink Theme

```jsx theme=pink
import React from 'react'

export function Counter({ initialCount = 0 }) {
	const [count, setCount] = React.useState(initialCount)
	return (
		<button type="button" onClick={() => setCount((prevCount) => prevCount + 1)}>
			{count}
		</button>
	)
}
```

## Turquoise Theme

```jsx theme=turq
import React from 'react'

export function Counter({ initialCount = 0 }) {
	const [count, setCount] = React.useState(initialCount)
	return (
		<button type="button" onClick={() => setCount((prevCount) => prevCount + 1)}>
			{count}
		</button>
	)
}
```

# Other features

Here are the other features like `lineHighlights`, `wordHighlights`, `showLineNumbers`, `RegisterLink` and `H`

## Line Highlights

If I want to select line 3:

```html line=3
<div>
	<h1>Hello world</h1>
	<p>This is a code block example.</p>
	<p>Do you like it?</p>
</div>
```

I can also use ranges:

```html line=2-4
<div>
	<h1>Hello world</h1>
	<p>This is a code block example.</p>
	<p>Do you like it?</p>
</div>
```

Or multiple unique lines:

```html line=1,5
<div>
	<h1>Hello world</h1>
	<p>This is a code block example.</p>
	<p>Do you like it?</p>
</div>
```

## Word highlights

```jsx
import React from '__react__'

function Hello() {
	return <h1>Hello world</h1>
}
```

## Show Line Numbers

```html showLineNumbers=true
<div>
	<h1>Hello world</h1>
	<p>This is a code block example.</p>
	<p>Do you like it?</p>
</div>
```

## RegisterLink

<RegisterLink id="demo" index="1" href="https://reactjs.org/" />

```jsx id=demo
import React from '__react__'

function Hello() {
	return <h1>Hello world</h1>
}
```

## Highlight

To highlight the words <H id="highlight" index="1">hover me</H>.

```jsx id=highlight
import React from '__react__'

function Hello() {
	return <h1>Hello world</h1>
}
```
